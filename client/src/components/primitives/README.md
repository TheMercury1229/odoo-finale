# Shared Primitives

New pages should compose these primitives rather than building layout and styling from scratch.

- Use `PageHeader` plus `DataTable` for new list pages.
- Use `FormActions` for consistent form footer actions.
- When adding a status value, add it to `STATUS_CONFIG` in `StatusBadge.tsx` instead of hardcoding badge colors inline.
